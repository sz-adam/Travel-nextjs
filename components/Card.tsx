import React from "react";
import { TravelItem } from "@/type/types";

interface CardProps {
  data: TravelItem; 
}

const Card: React.FC<CardProps> = ({ data }) => {
  return (
    <div>
     
    </div>
  );
};

export default Card;